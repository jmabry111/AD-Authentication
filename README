This is a basic php attempt to authenticate users with an Active Directory server. 

If the user authenticates with AD, it then checks a local database to see if the user is authorized to use the system. 
